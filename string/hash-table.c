char* add(char* key, char* value, char* hashTable[])
{
    int keyIndex = hashFunction(key);
    // TODO: add the value at the index
}

char* get(char* key, char* hashTable[])
{

}

int hashFunction(char* key)
{
    // TODO: do magic
    return 0;
}